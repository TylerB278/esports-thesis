---
const { folder } = Astro.props;
const { w } = Astro.props;
const { j } = Astro.props;

---

<img class="image-component" style=`width: ${w}; --just = ${j};` src=`/assets/${folder}/thumbnail.png`>
</img>

<style>
  .image-component {
    display: flex;
    flex-flow: row wrap;
    transition: all 0.2s;
    padding: 10px;
    margin: 0 auto;
    border-radius: 0px;
    /* max-width: 400px; */
  } .image-component:hover{ border-radius: 10%; }
</style>
